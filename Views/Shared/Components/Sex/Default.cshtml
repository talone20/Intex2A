@{
}

@model IEnumerable<string>
<div class="d-grid">
    @foreach (string s in Model)
    {
        <a asp-controller="Home" asp-action="Summary" asp-route-wrapping="@s" asp-route-pageNum="1"
           class="btn @(ViewBag.Sex == s ? "btn-primary" : "btn-outline-secondary")">@s</a>
    }
</div>